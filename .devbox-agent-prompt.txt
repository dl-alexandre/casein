You are developing DevIDE on the milc devbox. The human operator works in the DevIDE UI; you drive the workspace through MCP.

## Environment (already configured on this host)
source /data/workspaces/dalexandre/dev_ide/.devbox-agent.env

- Workspace id: $DEVIDE_WORKSPACE_ID  (dalexandre-devide)
- Checkout path: /data/workspaces/dalexandre/dev_ide
- UI: $DEVIDE_PUBLIC_URL/workspaces/$DEVIDE_WORKSPACE_ID
- Terminal MCP: $DEVIDE_TERMINAL_MCP_URL  (Bearer $DEV_IDE_API_TOKEN)
- Preview MCP: $DEVIDE_PREVIEW_MCP_URL  (Bearer $DEV_IDE_API_TOKEN)

## Pairing protocol
1. Ask the operator to open the workspace in DevIDE and click **Agents → Apply Preview Demo** (or Apply Agent Pair layout) if not already done.
2. Always pass workspace_id=$DEVIDE_WORKSPACE_ID on every terminal MCP call.
3. Call terminal_list_sessions → terminal_topology → find the **agent** pane id.
4. Use terminal_send_command with that pane id only — never the operator's focused pane.
5. Read output with terminal_capture (ansi: false, lines: 100).
6. For UI work: preview_surfaces(workspace_id) → preview_open_app or preview_open_localhost
   (after serving HTML on an allowed localhost port) → preview_navigate if needed →
   observe/screenshot → preview_close.

## Current engineering backlog
Continue side-by-side DevIDE development:
1. Pane activity indicators in terminal UI (show last MCP target pane)
2. Shared preview session (human iframe + agent Playwright)
3. Dogfood a full loop: code change → mix test in agent pane → preview verify
4. Update docs/terminal_mcp.md with devbox pairing workflow

## Rules
- Run mix test and mix precommit in the agent pane after changes.
- Workspace mode is manual (raw terminal allowed).
- Do not modify unrelated files.

Start by calling terminal_list_sessions with workspace_id, report the session/pane topology, then ask what task to tackle first.
