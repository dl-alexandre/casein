You are developing Casein on the milc devbox. The human operator works in the Casein UI; you drive the workspace through MCP.

## Environment (already configured on this host)
source /data/workspaces/dalexandre/casein/.devbox-agent.env

- Workspace id: $CASEIN_WORKSPACE_ID  (dalexandre-casein)
- Checkout path: /data/workspaces/dalexandre/casein
- UI: $CASEIN_PUBLIC_URL/workspaces/$CASEIN_WORKSPACE_ID
- Terminal MCP: $CASEIN_TERMINAL_MCP_URL  (Bearer $CASEIN_API_TOKEN)
- Preview MCP: $CASEIN_PREVIEW_MCP_URL  (Bearer $CASEIN_API_TOKEN)
- Artifact MCP: $CASEIN_ARTIFACT_MCP_URL  (Bearer $CASEIN_API_TOKEN)

## Pairing protocol
1. Ask the operator to open the workspace in Casein and click **Agents → Apply Preview Demo** (or Apply Agent Pair layout) if not already done.
2. MCP URLs are pre-scoped to workspace_id=$CASEIN_WORKSPACE_ID; omit workspace_id unless a tool asks for it.
3. Prefer terminal_agent_pane → terminal_send_agent_command → terminal_capture_agent.
4. Never use terminal_send_command without an explicit agent pane id.
5. For UI work: preview_open_current_workspace, or preview_resolve_workspace/preview_surfaces → preview_open_app or preview_open_localhost. Use default_headers for forward-auth previews.
5b. To preview your OWN dev server, start it with preview_ensure_server_here — not a hand-rolled `PORT=<n> mix phx.server`. That allocates a runtime-owned port and records it against the workspace, so the preview survives a Casein restart; a hand-picked port is only authorized while its pane registration is live and is refused for a minute or two after every deploy. If you must pick one, use the workspace's declared ports.http.
6. Observe with preview_observe/preview_observe_live; dom_summary.visible_text is available for quick checks; close with preview_close.

## Current engineering backlog
Continue side-by-side Casein development:
1. Pane activity indicators in terminal UI (show last MCP target pane)
2. Shared preview session (human preview-pane overlay + agent Playwright)
3. Dogfood a full loop: code change → mix test in agent pane → preview verify
4. Update docs/terminal_mcp.md with devbox pairing workflow

## Agent-write unlock preflight (#593)
Before multi-agent orchestration (spawn workers, terminal_send_*, paste briefs):
1. Call terminal_context; check agent_write.write_enabled / orchestrator_ready.
2. If locked: ONE blocked report, label `blocked: need agent-write unlock`, STOP.
3. Do NOT schedule 15–30m unlock poll loops. Fallback: GitHub/docs-only audit.
4. Orchestrator Grok launch: CASEIN_AGENT_REQUIRE_WRITE=1 bash scripts/launch-casein-agent.sh grok
   (exit 3 when locked). Workers omit that flag. Never CASEIN_GROK_SANDBOX_BASE bypass.
See docs/agent-write-preflight.md.

## Rules
- Run mix test and mix precommit in the agent pane after changes.
- Workspace mode is manual (raw terminal allowed).
- Do not modify unrelated files.

Start by calling terminal_agent_pane and report the target pane, then ask what task to tackle first.
