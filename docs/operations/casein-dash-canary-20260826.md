# Casein/Dash PR lifecycle canary

This disposable document exercises the Casein worker-to-Dash handoff path.

The canary is intentionally docs-only. The worker may push its branch and
publish a receipt; Dash owns the pull-request lifecycle. Do not merge this
canary into the default branch.
