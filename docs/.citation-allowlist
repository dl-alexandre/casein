# Module-like names cited in docs that legitimately have no `defmodule` in the tree.
# One per line. Used by scripts/check-doc-citations.sh. Keep this short and justified.

# Registered process / Registry / Supervisor names (started with `name:`, not defined as modules):
Casein.Push.APNS.Finch
Casein.TaskSupervisor
Casein.Terminals.Registry
Casein.Terminals.Supervisor
Casein.Terminals.TopologyRegistry
Casein.Terminals.SessionRegistry
Terminals.Registry

# Nested modules (defined as `defmodule System do` inside a parent; the bare
# defmodule line hides the parent namespace from the scanner):
Casein.Workspaces.SshRunner.System
SshRunner.System

# Adjacent native/mobile docs cite modules outside this Phoenix app's lib scan,
# plus Zig build API types.
Build.Module
Build.Step.Compile
CaseinMob.GhosttyVt
CaseinMob.HostBridge
CaseinMob.Nifs.GhosttyVt
CaseinMob.Terminal
# native/casein_mob is outside the lib/ scan (companion OTP app).
CaseinMob.Theme
# Historical pre-rename module name cited in theme-kit inventory (#740).
DevideMob.Theme
Mob.Dev
Mob.Dev.Connection
Mob.Dist
# Hex :mob theme API (deps/mob), not an app module.
Mob.Theme
MobDev.StaticNifs

# External hex dependencies cited in docs (not defined in this app's lib tree):
Mint.WebSocket
