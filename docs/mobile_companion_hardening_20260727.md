# Mobile companion hardening log — 2026-07-27

Status: complete with external physical-device blockers

## Scope

Bounded physical-device and real-usage soak from exact base
`08c44e701a1e0e7b5be34d879597ab72845217ae`. Devbox operator activity is
read-only observation. Active experiments use Local Mac or an explicitly
role-marked disposable agent pane after authoritative revalidation.

## Observations

| Time (UTC) | Scenario | Expected | Observed / evidence | Classification | Severity | Disposition |
|---|---|---|---|---|---|---|
| 14:18 | Devbox deployed baseline | Exact `origin/master` is live and the poller reports trustworthy health | Exact service `08c44e70…` and loopback `/healthz` are healthy, but every poller tick records `phase=caddy` failure because Caddy still uses the exact known legacy `/run/devide/current.sock` route | Product/operations defect | Medium | Fixed by merged PR #403: allowlist migration of only the exact legacy dial; unknown upstreams remain refused |
| 14:24 | Passive Devbox usage telemetry | Recovery and attention signals remain low-noise enough for companion triage | `terminal.session_recreated` fired 1,920 times in eight hours; two subscribed sessions each re-fired every 30 seconds, with matching template applications and degradation alerts. No operator output or pane content was inspected | Product defect | High | Root cause is the drift probe choosing host/container scope without the owner-bound workspace cwd. PR #404 scopes the read-only probe and preserves the existing attachment |
| 14:25 | Privacy-bounded mobile audit | Lifecycle telemetry is useful without content | Recent mobile observations recorded platform/outcome/fallback/stale bucket only; selected forbidden keys (`message`, `output`, token/credential, terminal output) were absent from all rows | Green | — | Preserve |
| 14:26 | Exact iOS signing/install | Current signed build installs on connected iPad/iPhone | Build/link/codesign succeeded, but install failed `0xe8008015`: auto-selected wildcard profile lacks `aps-environment`, while the app correctly requests development push entitlement | External configuration | — | Dedicated provisioning attempted |
| 14:27 | Apple provisioning recovery | Existing provisioning flow creates a dedicated push-capable profile | Apple rejected `mix mob.provision` with xcodebuild 65 because the account has an updated Program License Agreement awaiting acceptance | External blocker | — | Account owner must accept the Apple agreement; do not weaken app entitlements |
| 14:29 | Fresh Android install | An unpaired exact-source install opens the Action Center and pairing entry point | The BEAM and migrations started, but Android showed a permanent black canvas. Live root start failed in `Mob.Renderer.prepare/4` because the unpaired dashboard emitted `nil` as its first child when no saved hosts existed | Product defect | High | Fixed by PR #406 and physically revalidated on the SM-T390 |
| 14:31 | Android exact-source build | The supported native build packages the tracked host bridge unchanged | `mix mob.deploy --native` regenerated `MobNotifyBridge.kt` from the plugin template and silently removed committed FCM blank/error handling. The file was restored byte-for-byte before assembling the evidence APK | External dependency defect | Medium | Upstream `mob_notify` template/package alignment required; tracked as #407. No Casein-only preservation hack or weakened push behavior accepted |
| 14:32 | Standalone Android Gradle fallback | A documented standalone Gradle build remains a useful fallback | CMake still names removed Mob 0.7.5 C sources while the product now ships Zig replacements, so clean `assembleDebug` fails before the supported Mix/Zig build | Developer-tooling friction | Low | Fixed in #408: CMake consumes Zig `jniLibs`/`.o` artifacts or fails closed with `mix mob.deploy --native --android`; hermetic cmake-configure regression in `android_cmake_zig_fallback_test.exs` |
| 14:34 | Exact macOS packaging | A clean first package captures only the project version | The package script captured first-build compiler stdout into the archive filename, producing an overlong path. Sentinel-based extraction now rejects missing or multiple version lines and preserves Mix failures | Product packaging defect | Medium | Focused tests 4/4; exact Developer-ID archive built and isolated Finder/LAN/auth smoke passed |
| 14:35 | macOS trust | The Developer-ID archive is installable without misleading trust claims | Deep/strict codesign is green, but Gatekeeper reports `Unnotarized Developer ID` | External configuration | — | Keep explicit; do not claim notarization |
| 14:46 | Android fresh-install revalidation | The fix renders on the physical old-density target from exact tracked source | Installed/pulled APK SHA-256 `de575d…` is byte-identical to the local exact-source APK. The SM-T390 renders Action Center, “Not paired yet,” and `+ Pair workspace`; no fatal exception or ANR | Green | — | Landed and deployed through PR #406 |
| 14:48 | Android lifecycle/layout | Portrait, landscape, keyboard, scroll, warm, and killed-app flows remain understandable | Pair screen is usable in portrait and 1280×800 landscape; keyboard-obscured content is reachable by normal scroll. Warm resume is healthy. Force-stop cold launch succeeds in 9.620s without crash/ANR but stays black until the BEAM root is ready | UX friction | Low | Correctness green; native cold-start feedback tracked as #410 |
| 14:49 | Android deploy failure handling | Recoverable install failures preserve paired app data and fail visibly | Upstream `mob_dev` 0.6.23 broadly treated `INSTALL_FAILED_*` as clean-reinstall-worthy and could report success after a per-device failure; on a 96%-full device, insufficient storage could erase app data | External dependency defect | High | Fixed upstream in `mob_dev` (`7375287` / pin `433a9d09…`): update-only `adb install -r`, no uninstall fallback, classified failures, partial multi-device failures error. Casein contract tests lock the pin (#409). Physical redeploy-with-data survival was **not** re-run on this box (no attached Android device) |
| 14:50 | PR #403 normal deployment | The canonical poller activates the exact merge and repairs Caddy | Poller outcome `success` for `63789480…`; active service and `/healthz` are healthy; Caddy reports the canonical socket | Green | — | Landed and deployed |
| 15:06 | Session-drift fix landing | The owner-bound probe preserves each workspace's tmux source and stops the 30-second recreation loop | PR #404 merged at `0fefbb28…` after a green 12m15s exact-head gate. Focused overlapping tests passed 114/114; final diff remained five terminal-control files with no review threads | Green | — | Normal poller activated the exact merge at 15:16; `/healthz` is ready and a read-only audit count found zero `terminal.session_recreated` events after more than two 30-second drift intervals |
| 15:06 | Android fresh-dashboard fix landing | The physical fresh-install repair lands without unrelated native or generated files | PR #406 merged at `29acec75…` after a green 11m25s exact-head gate. Final diff remained the dashboard projection and its no-nil-child contract test; physical SM-T390 evidence is unchanged by the rebase | Green | — | Normal poller activated the exact merge before advancing to the next master revision |
| 15:07 | macOS package CI signal | A rebased packaging head receives an executable platform signal | GitHub's hosted macOS job never started (`runner_id=0`, no steps) and reported only the account billing/spending annotation | External infrastructure | — | Hosted job treated as inert; the exact-head self-hosted gate passed before merge |
| 15:46 | macOS packaging fix landing | The deterministic version-capture fix integrates with current master without widening scope | PR #405 merged at `2da0daac…` from exact reviewed head `c3185f35…` after a fresh green self-hosted gate. Two concurrent terminal-only master commits did not overlap the two-file packaging diff; the final hosted macOS job again had zero steps and only the billing annotation | Green | — | Exact landed package revalidation completed |
| 15:58 | Exact landed macOS artifact | A clean Developer-ID package from landed master has trustworthy identity and LAN behavior | Manifest revision `2da0daac…`, build 1559, version 0.1.0, arm64, tmux 3.7b. Archive SHA-256 `7952fddbe987524d56db7b1e31397cf61f8e90fd9d84a968e7d4f74badfc41ea`; strict/deep codesign, isolated Finder launch, bundled tmux, LAN health, auth 401/200, and replay-rejection smoke are green | Green | — | Gatekeeper remains explicitly `Unnotarized Developer ID` |
| 15:58 | Exact landed iOS artifact | Current source builds and signs for the connected iPad and iPhone without weakening push entitlements | A clean 180 MB app rebuilt at `2da0daac…`; executable SHA-256 `f88e9264b1cc117e695a4ef47d3fa173eb9a45cb19606ee5b5f17445a2ed7842`, strict/deep codesign green. The wildcard profile covers both UDIDs but lacks the required development `aps-environment`; the app is installed on neither device | External blocker | — | Accept the Apple agreement and create a dedicated push-capable profile before physical install/lifecycle/layout claims |
| 15:59 | Origin/intervention matrix | Pair Local Mac and Devbox through normal product controls | Fresh Android app is ready at the pairing screen, but the Mac remained locked and the isolated browser was neither allowed to open the LAN page nor signed into Devbox. No credential was copied into ADB or logs | External/operator availability | — | Resume after Mac unlock; do not synthesize or log a pairing credential |

## Remaining external blockers

- Accept the updated Apple Program License Agreement and create a dedicated
  `com.alexandrefamilyfarm.casein-mob` development profile with
  `aps-environment=development`.
- Unlock the Local Mac to display the normal authenticated pairing QR. This is
  required before origin switching, authoritative Evidence Handoff, and
  exact-agent intervention can be claimed on a physical device.
- Notarize the Developer-ID macOS archive before claiming Gatekeeper acceptance.
- APNs/FCM delivery credentials remain outside this bounded pass; no receipt was
  claimed.
