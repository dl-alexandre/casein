# Agent notes

Short, agent-facing contracts that are easy to load mid-session.

| Doc | When to read |
|-----|----------------|
| [`host-grok-dual-plane.md`](host-grok-dual-plane.md) | Host laptop Grok (or similar) with Casein MCP: dual-plane ops, product cwd (not `$HOME`), multi-workspace `workspace_id`+`session` discipline, mutate rules |

Full off-box wiring remains in [`../external-agent-connect.md`](../external-agent-connect.md).
