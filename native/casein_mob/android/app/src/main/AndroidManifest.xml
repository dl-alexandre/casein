<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.casein_mob">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Microphone (audio recording). CAMERA + the camera uses-features are
         declared by the mob_camera plugin's manifest and merged at build. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- USB host (Mob.Peripheral.VendorUsb). required=false so the Play Store
         doesn't filter the app off devices without USB host support; apps
         degrade via Mob.Peripheral.capabilities/0. No uses-permission needed —
         per-device access is gated by the runtime ACTION_USB_PERMISSION dialog. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Media-library read permissions moved to the mob_photos plugin manifest
         (READ_MEDIA_IMAGES / READ_MEDIA_VIDEO / READ_EXTERNAL_STORAGE). NOTE the
         plugin's plain-string permission list can't express maxSdkVersion="32"
         on the legacy one — harmless (no-op on API 33+). -->

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AlarmManager for scheduled local notifications -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:minSdkVersion="31" />

    <!-- Haptic feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Package visibility (API 30+): apps we want to launch via Mob.Device.open_url. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:label="Casein"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <!-- Main activity -->
        <activity android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboard|keyboardHidden|navigation|uiMode|fontScale|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="casein" android:host="review" />
            </intent-filter>
        </activity>

        <!-- Pairing links contain a short-lived bootstrap credential. Route
             them through a no-history trampoline so Android never retains the
             opaque code as MainActivity's task/base intent. -->
        <activity
            android:name=".PairingActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="com.example.casein_mob.pairing"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="casein" android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Full-screen QR scanner from the mob_scanner plugin. The Activity is
             plugin-owned, but Android still requires the host manifest entry. -->
        <activity
            android:name="io.mob.scanner.MobScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Scheduled local notification delivery -->
        <receiver android:name=".NotificationReceiver"
            android:exported="false" />

        <!-- Firebase Cloud Messaging push delivery for mob_notify. -->
        <service
            android:name=".MobFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Re-arm persisted local-notification schedules after reboot. -->
        <receiver
            android:name="io.mob.notify.MobNotifyBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for camera temp files (photo / video capture) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
