interface:
  display_name: "DevIDE Workspace Handoff"
  short_description: "Report durable agent worktree outcomes"
  default_prompt: "Use $devide-workspace-handoff to finish this agent session with a durable, explicit handoff."
