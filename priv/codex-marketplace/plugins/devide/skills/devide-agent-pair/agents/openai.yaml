interface:
  display_name: "DevIDE Agent Pair"
  short_description: "Control the dedicated agent pane safely"
  default_prompt: "Use $devide-agent-pair to inspect the tmux topology and work through the dedicated agent pane."
